DOM-Cache: tickDomCache in state.js — 8 getElementById + 1 querySelectorAll aus tick() eliminiert. Bei 60fps im Run-Modus ~540 unnötige DOM-Lookups/Sekunde eingespart. settingsInputs lazy-gecached (erster Tick initialisiert, danach Cache-Hit). fetchHealth/toggleStreamView nicht geändert (nicht im Tick-Loop).

Code-Review bestätigt: Null-Safety bewahrt, settingsInputs static NodeList (kein stale-Ref-Risiko), Style konsistent.
