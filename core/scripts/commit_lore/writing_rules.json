{
  "rules": {
    "workplace_tone": {
      "description": "Jeder Commit ist ein Arbeitstag im Repo. Beschreibe was gemacht wurde, warum, und was als naechstes kommt. Klar, praezise, menschlich. Kein Theater, keine vorgeschriebenen Toene. Das Repo ist ein Arbeitsplatz — die Commits fuehren Tagebuch ueber die Arbeit.",
      "required": true
    },
    "commit_diary": {
      "description": "Zusammenfassung der an diesem Commit erledigten Arbeit. Was wurde geaendert, warum, welche Auswirkung hat es?",
      "required": true,
      "min_words": {
        "STANDARD": 80,
        "TEST-ASSET": 60,
        "TRIVIAL": 20,
        "LORE-ONLY": 40
      },
      "stretching_policy": "Keine kuenstliche Streckung. Wenn kurz, dann kurz."
    },
    "model_signature": {
      "description": "Commit message muss den ausfuehrenden Agenten ausweisen.",
      "required": true,
      "token_format": "[MODEL:<model-name>]",
      "regex": "\\[MODEL:([a-z0-9._-]+)\\]"
    },
    "impulse_token": {
      "description": "Der [IMPULSE:<user-input>] Token dokumentiert den Auftrag der diesen Commit ausgeloest hat.",
      "required": true,
      "token_format": "[IMPULSE:<user-input>]",
      "regex": "\\[IMPULSE:(.{5,}?)\\]",
      "min_length": 5
    },
    "file_references": {
      "description": "Alle staged Files muessen in der Commit-Nachricht referenziert werden (basename oder stem reicht).",
      "required": true
    },
    "optional_elements": {
      "sidejoke": {
        "description": "Freiwillig: Ein Sidejoke aus dem Pool kann eingebaut werden.",
        "required": false,
        "pool_path": "core/scripts/commit_lore/sidejoke_pool.json"
      },
      "plotchain_reference": {
        "description": "Freiwaehlbar: Ein [REF:<id>] um thematisch an einen vergangenen Commit anzuknuepfen. Muss NICHT der letzte Node sein — jeder vergangene Node ist gueltig.",
        "required": false,
        "token_format": "[REF:<plot-id>]"
      },
      "arc_reference": {
        "description": "Freiwaehlbar: Verweis auf den aktuellen Versions-Arc oder einen archivierten Arc.",
        "required": false
      }
    }
  }
}
